// Hot path locked down so a future edit cannot silently regress it.
#checksum_no_alloc(n L) L = {
  acc L:= 0;
  i L:= 0;
  @i < n { acc = acc + i * 31; i = i + 1; acc };
  acc
};
#main() L = checksum_no_alloc(10);
