# Agent-authoring corpora ([T-8])

Three arms, reported separately ([T-8.1.1]):

1. **Public** — HumanEval / MBPP / Exercism ported specs
2. **Hidden** — procedural (`ax eval-loop`); the primary number
3. **Repo-edit** — multi-definition change tasks

The in-tree `eval-loop` is the hidden-arm harness. Public and repo-edit
corpora are acquired, not invented here.
