//! expect: 1usz
module conf.stdlib.vec_eq;
fn main() -> usz !{alloc[a]} = {
    let mut a: Vec[u8] = vec.new(test.alloc);
    let mut b: Vec[u8] = vec.new(test.alloc);
    a.push(1u8); a.push(2u8);
    b.push(1u8); b.push(2u8);
    if a.eq(b) { 1usz } else { 0usz }
};
