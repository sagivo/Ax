//! expect: 3i32
// Disjoint `par` is accepted (v0.3). Sequential evaluation is observationally
// identical when captures do not alias.
module conf.concurrency.par_rejected;
fn main() -> i32 = { par { let a = 1; let b = 2; }; 3 };
