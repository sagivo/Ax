//! expect: -2147483648i32
// The classic overflow case. In C this is undefined; Rust panics; Ax defines it
// to wrap, like every other `+ - *` overflow.
module conf.int.div_min_by_neg_one;
fn main() -> i32 !{err[DivError]} = int.div_trunc(-2147483648i32, -1i32);
