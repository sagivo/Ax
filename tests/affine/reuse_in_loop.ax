//@ id:        T-AFF-0004
//@ requires:  R-3.3.1
//@ origin:    authored — own reuse in a loop is A2020
//@ upstream:  none
//@ license:   MIT
//@ port:      authored
//@ expect:    error: A2020
module tests.affine.reuse_in_loop;
fn take(p: own i32) -> i32 = {
    let mut s: i32 = 0;
    for i in range(0, 2) { s = s + p; };
    s
};
fn main() -> i32 = 0;
