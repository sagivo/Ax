//@ id:        T-SND-0007
//@ requires:  R-5.2.3
//@ origin:    authored — [R-5.2.3] premise 3 (duplicate witness)
//@ upstream:  none
//@ license:   MIT
//@ port:      authored
//@ expect:    value: 5i32
module sound.read_no_drop;
fn twice(x: i32) -> i32 = x + x;
fn main() -> i32 = {
    let a: i32 = 5;
    let b = twice(a);
    a - b + b
};
