//@ id:        T-OWN-0002
//@ requires:  R-5.2.4, R-1.3.1
//@ origin:    authored — Escape → residual RC reported
//@ upstream:  none
//@ license:   MIT
//@ port:      authored
//@ expect:    compile
module tests.ownership.escape_forces_rc;
type R = { x: i32 };
fn wrap(v: R) -> R = v;
fn main() -> i32 = wrap(R { x: 1 }).x;
