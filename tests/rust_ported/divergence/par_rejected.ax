//@ id:        T-DIV-0003
//@ requires:  R-8.1.3
//@ origin:    documented: overlapping par `&mut` captures rejected (E0600)
//@ upstream:  none
//@ license:   MIT
//@ port:      authored
//@ expect:    error: E0600
module tests.rust_ported.divergence.par_rejected;
fn main() -> i32 = {
    let mut x: i32 = 0;
    par { let a = &mut x; let b = &mut x; };
    3
};
